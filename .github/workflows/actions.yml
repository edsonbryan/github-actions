name: First Action

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types:
      - opened

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: First Step
        shell: bash
        run: |
          echo "running first step"
