name: First Action

on:
  push:
    branches:
      - main
      - feature/**
  pull_request:
    types:
      - opened
      - edited

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: First Step
        shell: bash
        run: |
          echo "running first step"
